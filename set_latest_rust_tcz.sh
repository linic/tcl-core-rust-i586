#!/bin/bash
# NOTE: run it with a '.' before the script name. Example:
# . /path/set_latest_rust_tcz.sh
# This runs the script in the current shell and makes the variable available to other scripts which will
# be run in this shell. Use it with sudo when you'll use the copy_tcz_locally.sh script after with sudo too.
export latest_rust_tcz=rust-1.81.0-i586.tcz

